# EOD note — 2026-05-06

Day-3 continuation from 2026-05-05. Two production launch-blockers verified closed; F-256 Phase 1 investigation shipped with major insight.

## Verified closed today

- **F-247** — Bill view crash. Tester PASS in production confirmed via WhatsApp. Backlog entry marked CLOSED in commit `c305487`.
- **F-257** — Hardcoded currency in Wastage Control + Shift Reconciliation. Tester PASS confirmed across Wastage page (4/4 tests) and Shift Reconciliation. Backlog entry marked CLOSED in commit `c305487`.

## Shipped today

- F-256 cross-reference cleanup (`cac2417`). Removed stale F-257 reference from F-256 backlog entry.
- F-247/F-257 backlog closure (`c305487`). Both entries prepended with [CLOSED 2026-05-06] tag.
- F-256 Phase 1 static investigation (`901ca5d` on branch `fix/F-256-phase1-investigation`). 361-line findings file documenting the data-flow trace, three-bug split, and Phase 2 recommendations.
- F-256 backlog Phase 1 update (`81ec732`). Sub-bullet added under F-256 entry referencing Phase 1 findings file and the three-bug split.

## Key insight from F-256 Phase 1

F-256 is NOT one bug. It is three separate bugs masquerading under one symptom (settings revert on refresh):

- **Subscription Plan revert** — WRITE-SIDE bug, fully diagnosed. Save handler `handleBusinessConfigSubmit` omits `plan` from PATCH payload, AND server `OWNER_EDITABLE_FIELDS` allowlist intentionally blocks `plan` per F-023 security fix. The dropdown is fundamentally non-functional. Phase 2 fix shape: code-only, remove the dropdown, plan changes belong in `subscription-settings.tsx` Stripe path.
- **Currency revert** — persistence path looks correct from static reading. Tester reports "rest of the system continues using KRW" which means the write IS persisted. Bug is render-side or stale-cache. Phase 2 needs production recon (DevTools network panel + React state inspection) before any code change.
- **Time Zone reset to browser** — persistence path looks correct from static reading. No code path auto-populates browser TZ on mount. Leading hypothesis: tester is clicking adjacent "Reset to Browser" button instead of "Save Time Zone". Phase 2 needs tester clarification or screen recording before any code change.

F-258 should fold into F-256 (same Currency three-state symptom, same investigation covers both).

## Other housekeeping

- Two merged feature branches deleted locally and on GitHub: `fix/F-247-bill-view-qrdataurl-scope` and `fix/F-257-tenant-currency-wastage-shift`.
- Branch `fix/F-256-phase1-investigation` retained on origin awaiting Phase 2 decisions.
- Stale auto-generated `missing_columns.sql` deleted from repo root (was outside audit/ structure, gitignored anyway).
- Two intentionally-untracked audit SQL files remain on local disk: `audit/cashier-users-query-2026-04-21.sql` and `audit/f225-day4-audit.sql`. Both verified to contain no PII (read-only audit queries, header-comment only). Covered by `.gitignore *.sql` rule. No commit needed.
- `audit/cashier-seed-preflight-2026-04-21.sql` verified safe — already tracked in `e20e473`, contains test fixture email tied to fictional restaurant "grandkitchen", not real PII.

## Pending

### F-256 Phase 2 decisions (split into three sub-tracks)

- **F-256a (Subscription Plan):** code-only fix on a fresh branch off `fix/F-256-phase1-investigation`. ~25 lines removed + 1 i18n key cleanup. Single-file change in `client/src/pages/modules/settings.tsx:573-597`.
- **F-256b (Currency):** production recon. Open Settings page with DevTools network panel, save KRW, refresh, capture (a) `GET /api/tenant` response body, (b) React state at first paint, (c) React state after `useEffect` fires. Three possible outcomes documented in Phase 1 findings.
- **F-256c (Time Zone):** tester clarification first. Specifically Q-F256-1: is the tester clicking "Reset to Browser" instead of "Save Time Zone"? Need screen recording or direct confirmation. After that decide if it's a UX fix or a code fix.

Order recommendation TBD — discuss with user tomorrow morning before kicking off Phase 2.

### Other pending

- F-234 Phase 4 (cross-user table claim race condition). Spec already written: `audit/f234-phase3-decision-memo-2026-05-04.md`. Requires hand-written SQL migration via TablePlus before merge.
- F-254 (429 rate-limit storm). BLOCKING-tier. Phase 1 investigation not yet started.
- F-255 (GET /api/security-alerts/unread-count 404). ANNOYING tier.
- F-253 (POST /api/packing/calculate 400 errors on every Bill Preview).
- F-258 (Currency 3-way state mismatch) — folds into F-256b per Phase 1 findings.
- F-259 (QR Request settings changes don't propagate).
- F-260 (Default language UX inconsistencies). COSMETIC.
- F-261 (Shifts page function/usage unclear).

### Config audit findings still open

A-05 (uploads volume), A-06 (compose resource limits — stashed), A-07 (Redis), A-08 (gitignore typo), A-09 (tsconfig strictness), A-10 (vite overlay), A-11 (vite dev server binding), A-12 (compose version field), A-13 (server/public gitignore), A-14 (QR public surface audit).

### Previously flagged but unfixed

F-122 (bill tamper), F-123 (float drift), F-124 (manual discount cap), /legal/privacy and /legal/terms 404s.

## Local working state

- Active branch: `main` (HEAD will be the EOD commit after this lands).
- Working tree: clean apart from two pre-existing intentionally-untracked audit SQL files (covered by `.gitignore *.sql`).
- Local merged branches deleted: `fix/F-247-bill-view-qrdataurl-scope`, `fix/F-257-tenant-currency-wastage-shift`.
- Open branches: `main`, `fix/F-256-phase1-investigation` (Phase 2 will branch from this).
- Stashes: 2 (A-06 mem_limit/cpus on Railway-cert branch; F-222-FU-seed-env-guard WIP).
- No outstanding rollbacks, force-pushes, or open incidents.

## Day-3 mood and energy

Long day with a mid-day lunch break. Three near-misses caught and corrected: queued-prompt drift (typed "commit those untracked SQL files" reflexively, paused before sending, did sanitized inspection instead), branch deletion confusion (assumed stale info, verified actual state in GitHub UI before deleting), and post-meal scope creep (wanted to start Phase 2 but stayed on Day-3's planned tail tasks).

Productive ~6 hours total across morning verification work and afternoon Phase 1 investigation. Decision fatigue showed up in the file-write tooling struggle (4 attempts to write the Phase 1 findings file before it landed cleanly via heredoc chunking). Not catastrophic — the analysis itself was sharp throughout.

Tomorrow's note: F-256 Phase 2 sub-track ordering is the first strategic decision. Discuss with user before kicking off any Phase 2 work.

## Tomorrow's first actions

1. Standing brief refresh — read this note, check WhatsApp for any tester reports.
2. F-256 Phase 2 conversation with Claude (chat) — decide order of F-256a / F-256b / F-256c tracks. Recommendation: start F-256a (code-only, fully diagnosed, lowest risk) to ship a small win and unblock the rest later.
3. If F-256a goes well: branch `fix/F-256a-remove-plan-dropdown` off `fix/F-256-phase1-investigation`. Single-file change. Test, PR, merge.
4. F-256b production recon coordinates with testers — needs them logged in with DevTools open.
5. F-256c needs WhatsApp clarification with testers about button confusion.

## Priorities (mine, to be revisited)

1. Get the product to a "first paying restaurant could use it" state.
2. Fix BLOCKING bugs that prevent end-to-end testing.
3. Build legal pages (privacy, terms) for UAE + India (DPDP Act).
4. Complete functional correctness items (F-122/123/124).
5. Complete QR public surface audit (A-14) before external guest traffic.
6. Polish remaining audit findings opportunistically.

Audit findings that don't directly block launch can wait.
