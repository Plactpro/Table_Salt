# EOD note — 2026-05-07

Day-4 continuation from 2026-05-06. Two PRs merged to main, one production fix verified live, one audit-trail merge of Phase 1 docs. Backlog updated with F-254a closure sub-bullet, F-254b and F-254-FU follow-ups filed.

## Shipped to production today

- **F-254a** — H-1 fix for the rate-limiter ordering bug. Mounted apiLimiter, uploadLimiter, and authLimiter inside a new `setupRateLimits(app)` async function in `server/security.ts`, called with `await` from `registerRoutes()` in `server/routes.ts` AFTER `setupAuth(app)`. Now `req.user` is populated when keyGenerators read it, so user-keyed rate limits work correctly. Fixed three files (`server/security.ts`, `server/routes.ts`, `server/index.ts`). Also corrected stale "AWS ALB target group health checks" comment to "platform health probes" at `server/index.ts:138`. PR #23 squash-merged as `ec7a0df`. Verified live by my own smoke checks (login + API headers present + Settings/POS/Kitchen pages load) and by both testers (Madhesh and Nandhini, all 5 checks PASS).

## Shipped today

- `ec7a0df` — fix(F-254a): mount rate limiters after passport.session so user-keyed limits work correctly (squash-merge of PR #23 from `fix/F-254a-rate-limiter-ordering`)
- `86c7656` — Merge pull request #24 from Plactpro/fix/F-254-phase1-investigation (audit: F-254 Phase 1 investigation + review addendum). Preserves three SHAs: `3de022c` (Phase 1 header + TL;DR), `815bb82` (Phase 1 ops 2-4 body), `d335235` (Review Addendum)
- Backlog and EOD note updates pending commit on `chore/2026-05-07-eod-cleanup`

## Key insight from F-254a review

The Review Addendum (165 lines, written manually after Claude Code's Write tool kept corrupting it on five attempts) caught three errors in the Phase 1 doc that I would have missed otherwise: (1) `uploadLimiter` had the same H-1 bug as `apiLimiter` but Phase 1 didn't flag it; (2) the H-5 confirmation step required an `X-Process-Id` header that doesn't exist in the codebase; (3) the AWS ALB reference traced to a stale code comment, not invented architectural confusion.

The dual-read approach (independent analysis from me + Claude Code, cross-checked) caught one more issue at the implementation gate: `authLimiter` couldn't stay in `setupSecurity` if Redis init moved out, because the `redisStore` reference would be undefined. Switched to Option 2 — move all three limiters together. Fix shipped clean.

Tester captures from today (Madhesh and Nandhini, ~60 minutes recording each) showed zero 429s in any block. The fix is correct on first principles regardless. The captures are filed for reference; F-256b (Currency empty string, confirmed by both testers) and F-256c (Time Zone revert, confirmed by both testers) are now well-evidenced for tomorrow's work.

## Other housekeeping

- Backlog updated: F-254 parent entry's stale file-location text corrected (`server/middleware/rate-limit*.ts` → `server/security.ts`), F-254a closure sub-bullet added inline mirroring F-256a's nested style, F-254b and F-254-FU appended to FOLLOW-UP tier
- F-254a fix branch (`fix/F-254a-rate-limiter-ordering`) deleted on origin after merge
- F-254 Phase 1 investigation branch (`fix/F-254-phase1-investigation`) deleted on origin after merge
- All three commits on the Phase 1 branch preserved on main thanks to "Create a merge commit" merge strategy on PR #24 (correcting the squash-merge precedent set by PR #23 — for code fixes, squash is fine; for multi-commit doc deliverables, merge commit preserves SHAs cited in the docs themselves)

## Pending

### F-256b and F-256c
Both bugs confirmed by independent two-tester captures today. F-256b (Currency dropdown empty + React state empty string) and F-256c (Time Zone reverts after save + hard refresh) both reproduced. F-256b Phase 2 prep doc is parked on `fix/F-256b-phase2-prep` branch (commit `eac8b12`, not yet merged) — captures from today land on top of yesterday's H1-H4 hypothesis decision tree cleanly. Path B implementation (commit to one hypothesis based on captures) is tomorrow's first task.

### F-254 Phase 2
H-1 confirmed and fixed in F-254a. H-2 through H-5 remain unconfirmed. They may dissolve now that per-user budget is restored — most of the original 429 storm could have been H-1 amplified by IP sharing in the testers' office. Phase 2 observation-only captures still planned but lower priority post-fix. New tester questions QA-A through QA-F are filed in the addendum for when Phase 2 happens.

### F-254-FU and F-254b (newly filed)
Both LOW severity, in FOLLOW-UP. F-254b is a single-line client change (impersonation poller `enabled` gate). F-254-FU requires structural restructuring of `server/index.ts` middleware order — not a one-line move. Both deferred indefinitely until prioritized.

### Other pending
- Stripe live keys not configured (yellow banner + red toast pre-existing) — launch-blocker for paying customers
- Legal pages `/legal/privacy` and `/legal/terms` 404 — launch-blocker (UAE + India DPDP Act compliance)
- F-234 Phase 4 (cross-user table claim race) — needs hand-written SQL migration
- F-122/123/124 functional correctness items
- A-14 QR public surface audit — must complete before external guest traffic
- Multiple BLOCKING/ANNOYING/COSMETIC tier bugs from earlier project notes still open

### Config audit findings still open
A-05 (uploads volume), A-06 (compose resource limits), A-07 (Redis), A-08 (gitignore typo), A-09 (tsconfig strictness, 5 iterations), A-10 (vite overlay), A-11 (vite dev server binding), A-12 (compose version field), A-13 (server/public gitignore).

## Local working state

- Active branch: `chore/2026-05-07-eod-cleanup` (this branch)
- Tracked changes pending commit: `audit/00-backlog.md` (modified), `audit/eod-note-2026-05-07.md` (new)
- Untracked: `audit/cashier-users-query-2026-04-21.sql`, `audit/f225-day4-audit.sql` (pre-existing, intentionally not staged)
- Stashes: 0
- Deleted on origin today: `fix/F-254a-rate-limiter-ordering`, `fix/F-254-phase1-investigation`
- Parked on origin: `fix/F-256b-phase2-prep` (commit `eac8b12`, awaiting tester captures applied tomorrow)
- No outstanding force-pushes, rollbacks, or incidents

## Day-4 mood and energy

Long day, productive day, two genuine wins shipped. Started 9 AM IST with tester recon coordination, ended ~2 PM IST with backlog cleanup. Energy was good through the F-254a fix workflow — gate-by-gate review caught real issues (the authLimiter Redis-store dependency at the implementation gate, the squash-vs-merge-commit choice on PR #24, the truncated diff verification loops).

Process near-miss: Claude Code's Write tool corrupted the F-254 Phase 1 review addendum on five separate attempts before I gave up and wrote the 165-line file manually in Notepad. Lesson: when corruption recurs across fresh sessions with verified-clean buffers, switch tools fast rather than debugging the tool. Manual write took 5 minutes after I committed to it. Hours of recovery work was wasted before I made that call.

The "show me the full proposed state before applying" loop fired three times today (security.ts post-edit projection, addendum content, F-254 backlog parent entry) and caught real issues each time. Pattern is staying.

The corruption pattern needs a permanent workaround — for any markdown file over ~150 lines with multiple H2/H3 sections, write manually outside Claude Code from the start. Don't even attempt the Write tool. The cost of attempting is too high, the cost of writing manually is 5 minutes.

The tester redirect message (Block 3.5 same-role pairing per U-5) sent at the right time. Both testers caught the change before reaching that block. Worth remembering: when an analysis surfaces a methodology issue mid-test, send the redirect immediately rather than waiting for the next round.

The wrong squash-merge on PR #23 versus the correct merge-commit on PR #24 — split decision worked out fine. F-254a's pre-merge SHA disappeared from main when squashed into `ec7a0df`, but the addendum cites `d335235` (still preserved on main via PR #24's merge commit), and the F-254a closure sub-bullet in the backlog cites the merged SHA (`ec7a0df`) plus PR #23 for traceability. Audit trail intact.

## Tomorrow's first actions

1. Read both tester capture files in detail (`F254-recon-MADHESH.xlsx`, `F254-recon-NANDHINI.xlsx`) with focus on F-256b/F-256c findings rather than F-254 (which is solved). Block 1 captures confirm currency empty string and time zone revert.
2. Open `fix/F-256b-phase2-prep` branch's prep doc, layer today's tester findings on top of the H1-H4 decision tree, choose hypothesis to commit to. Likely H1 (initial-render race) or H4 (cache hydration order) based on Block 1 evidence.
3. Draft F-256b Phase 2 implementation plan on a new branch `fix/F-256b-phase2-impl`. Single-bug-per-branch discipline.
4. Decide whether to bundle F-256c (Time Zone) with F-256b or split into separate fix. Both share the Settings page read-back pattern but the bugs may have different root causes.
5. If energy permits: open F-254a-impersonation work (single-line change in `client/src/lib/impersonation-context.tsx`, ~15 minutes including verification).

## Priorities (mine, to be revisited)

1. Get the product to a "first paying restaurant could use it" state
2. Fix BLOCKING bugs that prevent end-to-end testing (F-256b/c are the live ones; F-254a closed today)
3. Build legal pages (privacy, terms) for UAE + India (DPDP Act)
4. Complete functional correctness items (F-122/123/124)
5. Complete QR public surface audit (A-14) before external guest traffic
6. Polish remaining audit findings opportunistically
