Add the following to the previous Replit prompt as an extra requirement block.

***

### Contact Sales – Inquiry Sheet & Email Routing

Add a **“Contact Sales”** flow tightly integrated with the animated chef and the web app UI.

**1. Entry points**

- A fixed **“Contact Sales” button** near the chef mascot in the bottom-left area (e.g., above or beside the chef).  
- Additional entry point in the main navigation or header labeled **“Contact Sales”**.  
- On click, open a **modal dialog** or slide‑in panel containing the inquiry form.

**2. Inquiry Form Fields**

Create a clean, validated **Contact Sales form** with:

- Full Name (required)  
- Business Name / Restaurant Name (required)  
- Business Type (dropdown: Enterprise, QSR, Food Truck, Cafe, Fine Dining, Casual Dining, Cloud Kitchen, Other)  
- Number of Outlets (dropdown or numeric input)  
- Country / City (required)  
- Email Address (required, email validation)  
- Phone / WhatsApp Number (optional but validated)  
- Preferred Contact Method (dropdown: Email, Phone, WhatsApp)  
- How did you hear about us? (dropdown: Web Search, Referral, Social Media, Other)  
- Subscription Interest (checkbox group: Basic, Standard, Premium, Enterprise, Not Sure)  
- Message / Requirements (multiline textarea, required, min length)  
- Optional checkbox: “I’d like a product demo”  
- Optional checkbox: “Subscribe to updates and offers”

**3. Validation & UX**

- Inline validation: show clear error messages under each invalid field.  
- Disable the **Submit** button until required fields are valid.  
- Show a subtle loading state while sending.  
- On success, show a **thank‑you message** and trigger a happy reaction from the chef (e.g., celebratory animation).  
- On error, show a friendly error toast and trigger a “worried → reassuring” reaction from the chef.

**4. Email Routing Logic**

- All form submissions must be **sent to a configurable sales email address** (e.g., `SALES_EMAIL` variable).  
- Implement a clear abstraction for sending emails so it can later be wired to a real backend (SMTP, API, etc.).  
- For now, simulate or stub the email send with a function like:

```javascript
// Pseudo-interface – implement real email/API later
async function sendContactSalesEmail(formData) {
  // formData contains all fields above
  // Use a configurable SALES_EMAIL constant
  const SALES_EMAIL = 'sales@yourdomain.com'; // make this easily changeable
  // In real app: call backend/email service here
  // For prototype: log to console and resolve as success
}
```

- The email content should include:
  - All captured fields in a **well‑formatted summary**.  
  - Timestamp and source page (if available).  
  - Browser / device info if easy to capture.

**5. Configuration & Extensibility**

- Store the target sales email address and optional CC/BCC emails in a **single configuration object** so it can be changed without editing core code.  
- Allow a flag like `ENABLE_CONTACT_SALES` to hide/show the feature.  
- Structure code so the same form can later:
  - Post data to a CRM or ticketing system API.  
  - Trigger automated follow‑up emails to the inquirer.

**6. Visual & Thematic Integration**

- Style the modal/panel with the same **premium tropical theme**:
  - Soft aqua background, white card, rounded corners, gentle shadows.  
  - Primary buttons in teal; secondary buttons in outline style.  
- When the form opens, the chef should:
  - Turn toward the form and do a small **“inviting” gesture** (hand wave pointing to the form).  
- On successful submission:
  - Chef plays a **celebration animation** (clapping, thumbs up, confetti).  
- Ensure everything is responsive and usable on mobile (full‑screen modal with larger tap targets).

**7. Success & Error Handling**

- **Success state:**
  - Clear confirmation message:  
    “Thank you! Our sales team will contact you shortly.”  
  - Option to **“Send another inquiry”** or **“Close”**.  
- **Error state:**
  - Human‑readable error message:  
    “We couldn’t send your inquiry right now. Please try again in a moment or email us at SALES_EMAIL.”  
  - Do not lose the user’s typed data on error; allow resubmit.

**Goal:**  
Provide a polished, production‑ready **Contact Sales** experience with a detailed inquiry form whose submissions are routed to a configurable sales email address, fully integrated with the animated chef mascot and the overall restaurant management web app theme.