Here is the complete, detailed prompt ready to paste directly into **Replit AI Planning Mode**:

***

```
## PROJECT CONTEXT

This is "Table Salt" — a multi-role SaaS Restaurant Management Web Application built on Replit. The app is live at https://inifinit.com/ and has been fully QA tested across all roles (Owner, Manager, Waiter, Kitchen, Accountant, Delivery Agent). The test was executed on 24 March 2026.

The following bugs were discovered during real-time testing and need to be fixed. Please enter Planning Mode, analyze the full codebase, identify the root cause of each bug listed below, and produce a detailed fix plan with exact file names, functions, and code changes required. Do NOT make any changes yet — only plan.

---

## BUG FIX REQUESTS — PLANNING MODE

---

### BUG-01 [CRITICAL] — /tables Page Crashes with "Something Went Wrong"

**Route:** /tables
**Affected Roles:** Owner, Manager, Waiter (all roles)
**Symptom:** When any user navigates to the Tables page (/tables), the page renders a crash error boundary showing "Something went wrong". Clicking "Try Again" / Refresh does not resolve it. The error persists on every load.

**Root Cause to Investigate:**
- Check the Tables page component (likely `Tables.jsx` or `tables/index.jsx`) for unhandled null/undefined errors — most likely a `.map()` or `.filter()` call on a null API response
- Check if the API endpoint that fetches tables/floor plan data is returning an error, empty array, or null and whether the component handles that gracefully
- Check if there is a missing `outlet_id` or `restaurant_id` context being passed to the tables fetch function
- Look for any missing database seed data for the "tables" collection/table in the demo environment

**Expected Fix:**
1. Add a null/empty array guard before any `.map()` on the tables data
2. Add a proper empty state UI: "No tables configured yet. Add your first table." with an "Add Table" CTA button
3. Wrap the page in an error boundary with a user-friendly fallback instead of a generic crash
4. Ensure the tables fetch API returns `[]` (empty array) instead of `null` or throwing a 500 when no tables exist
5. Do NOT show a crash — show an empty state

---

### BUG-02 [CRITICAL] — POS Table Selector Shows "No Tables Configured"

**Route:** /pos
**Affected Roles:** Waiter, Manager, Owner
**Symptom:** When a user selects "Dine-in" order type in the POS and clicks the table selector button, a modal opens but shows "No tables configured" — meaning the POS is pulling zero tables from the same broken data source as BUG-01.

**Root Cause to Investigate:**
- The POS table picker modal likely calls the same tables fetch function or API endpoint as the /tables page
- If BUG-01 is fixed (tables endpoint returns [] gracefully), this bug will partially resolve itself
- Additionally verify the POS table selector modal renders an "Add Tables First" prompt with a link to /tables when the list is empty, rather than just a blank/broken state

**Expected Fix:**
1. Fix is dependent on BUG-01's API fix — once tables endpoint returns proper data, POS will populate
2. Add a fallback message in the POS table picker: "No tables found. Please configure tables in the Tables module first." with a link/button to /tables
3. Disable the "Dine-in" tab or show a tooltip warning if zero tables are configured, to prevent user confusion

---

### BUG-03 [CRITICAL] — Dine-in "Send to Kitchen" Blocked Due to No Table Selected

**Route:** /pos
**Affected Roles:** Waiter, Manager
**Symptom:** When order type is "Dine-in" and user clicks "Send to Kitchen", a toast error fires: "Select a table — Choose a table for dine-in orders". This is a cascading failure from BUG-01 and BUG-02 — the user cannot select a table because none are loaded.

**Root Cause to Investigate:**
- The validation logic in the order submission function correctly requires a table for dine-in orders
- The problem is upstream: tables are not loadable due to BUG-01
- However, also check if there is a secondary validation that runs BEFORE checking if tables exist — if no tables are configured, the validation should prompt the user to configure tables rather than showing an ambiguous "select a table" error

**Expected Fix:**
1. This bug resolves once BUG-01 and BUG-02 are fixed
2. Additionally improve the error message: if tablesList.length === 0, show: "No tables are set up yet. Please configure your floor plan in Tables settings before placing dine-in orders." instead of the generic "select a table" toast

---

### BUG-04 & BUG-05 [HIGH] — POS Cash Payment: "Exact" Button Fails + Decimal Amount Validation Bug

**Route:** /pos → Pay button → Collect Payment modal
**Affected Roles:** Waiter, Manager
**Symptom 1 (BUG-04):** The "Exact" quick-fill button in the Cash Tendered section of the Collect Payment modal does not work correctly. Clicking "Exact" fills the input field with the order total (e.g. 35.66) but the validation still shows "Amount is less than total" and the "Confirm & Place Order" button remains grayed out/disabled.

**Symptom 2 (BUG-05):** Manually typing the exact decimal total (e.g. "35.66") into the Cash Tendered field also triggers "Amount is less than total". Only typing a whole number strictly greater than the total (e.g. "40") passes validation. This means decimal cash amounts are completely broken.

**Root Cause to Investigate:**
- This is almost certainly a floating-point / string comparison bug in the cash validation logic
- Look for the cash tendered validation function — it likely does something like: `if (cashTendered < orderTotal)` where one or both values are strings instead of numbers, OR the comparison uses strict equality on floats (e.g. `35.66 < 35.66` evaluating incorrectly due to floating point)
- The "Exact" button likely sets the value as a string `"35.66"` and the comparison `"35.66" < 35.66` returns true in JavaScript loose comparison
- Also check if `parseFloat()` or `Number()` is applied to BOTH values before comparison
- Look for the validation in the payment modal component — likely `PaymentModal.jsx` or `CollectPayment.jsx`

**Expected Fix:**
1. In the cash validation function, ensure BOTH values are parsed to float before comparison:
   ```javascript
   const tendered = parseFloat(cashTendered);
   const total = parseFloat(orderTotal);
   const isValid = tendered >= total;
   ```
2. Use `toFixed(2)` normalization on both values to avoid floating point drift:
   ```javascript
   const isValid = parseFloat(parseFloat(cashTendered).toFixed(2)) >= parseFloat(parseFloat(orderTotal).toFixed(2));
   ```
3. Fix the "Exact" button to set the value as a proper number, not a string:
   ```javascript
   setCashTendered(parseFloat(orderTotal).toFixed(2));
   ```
4. After fix, the "Exact" button should: fill the field with the exact total, show "Change Due: AED 0.00", and enable the Confirm button

---

### BUG-06 [HIGH] — KDS "Confirm & Start Cooking" Opens about:blank#blocked (Popup Blocked)

**Route:** / (KDS dashboard for Kitchen role)
**Affected Roles:** Kitchen Staff (Pat Garcia)
**Symptom:** When a kitchen staff member clicks "Start All" on a ticket in the NEW column → the "Recipe Check — Start Cooking" modal appears → clicking "Confirm & Start Cooking" causes the browser to navigate to `about:blank#blocked`. The KDS becomes unusable and the user must navigate back manually.

**Root Cause to Investigate:**
- The "Confirm & Start Cooking" action likely attempts to open a new browser popup/window for KOT (Kitchen Order Ticket) printing using `window.open()`
- Modern browsers block `window.open()` calls that are not directly triggered by a synchronous user gesture, OR when called inside an async function/promise chain
- The sequence is: click button → async API call to mark order as cooking → THEN `window.open(printUrl)` — the popup is blocked because the `window.open()` is inside an async callback, not the direct click handler
- Look for `window.open()` in the KDS component or a `printKOT()` / `printTicket()` utility function

**Expected Fix:**
1. **Preferred solution — In-app print instead of popup:** Replace `window.open(url)` with an in-app print approach using a hidden `<iframe>` or a React print library like `react-to-print`:
   ```javascript
   // Instead of window.open(kotPrintUrl)
   // Render KOT content in a hidden div and call window.print() directly
   const printRef = useRef();
   const handlePrint = useReactToPrint({ content: () => printRef.current });
   ```
2. **Alternative solution — Open popup synchronously first:** If keeping `window.open()`, open the window synchronously on button click (before the async call), then write content into it after the API resolves:
   ```javascript
   const printWindow = window.open('', '_blank'); // opens synchronously on click
   await confirmCooking(ticketId); // async API call
   printWindow.document.write(kotHtmlContent); // write content after
   printWindow.print();
   ```
3. After the fix, clicking "Confirm & Start Cooking" should:
   - Move the ticket from NEW → COOKING column in the KDS
   - Either print the KOT in-app or open a printable receipt without navigating away
   - Show a success toast: "Order #XXXX moved to Cooking"

---

### BUG-07 [MEDIUM] — Ticket History Shows "No Tickets Found" Despite 60+ Orders Existing

**Route:** /tickets
**Affected Roles:** Accountant (Morgan Lee), Waiter (all)
**Symptom:** The Ticket History page shows "No tickets found" for all quick filters: Today, Yesterday, This Week. This occurs even though:
- The Orders module (/orders) shows 50+ orders
- The Accountant dashboard shows 61 orders and AED3985.87 revenue
- A fresh order was placed during testing and confirmed with a Bill Preview

**Root Cause to Investigate:**
- The Ticket History page likely queries a different data source or table than the Orders page — e.g. it may query a `tickets` collection/table while orders are stored in an `orders` collection
- Check if there is a step in the order flow that is supposed to CREATE a ticket record (insert into `tickets` table) when an order is placed/completed — this step may be missing or broken
- Alternatively, the Ticket History date filter may have a timezone bug — the `createdAt` timestamps may be stored in UTC while the filter compares against local IST (+5:30), causing all records to appear outside the selected range
- Also check if the "Today" filter uses `new Date().toISOString().split('T')[0]` vs a proper timezone-aware date comparison

**Expected Fix:**
1. Check if tickets are supposed to be created automatically when an order is placed (via POS or Online Orders) — if yes, find and fix the missing `createTicket()` call in the order placement flow
2. If tickets = orders (same data), fix the Ticket History query to point to the correct `orders` collection with proper status filter (completed/paid orders)
3. Fix date range filtering to use timezone-aware comparison:
   ```javascript
   // Use date-fns or dayjs with timezone
   const startOfToday = dayjs().tz('Asia/Dubai').startOf('day').toISOString();
   const endOfToday = dayjs().tz('Asia/Dubai').endOf('day').toISOString();
   ```
4. After fix, Ticket History for "This Week" should show all 60+ orders with their Order ID, Table, Waiter, Amount, Payment Method, and Date

---

### BUG-08 [MEDIUM] — Delivery Agent Accounts (delivery1, delivery2, delivery3) Cannot Login

**Route:** /login
**Affected Roles:** Delivery Agent
**Symptom:** Attempting to login with credentials `delivery1 / demo123`, `delivery2 / demo123`, and `delivery3 / demo123` all return "Login failed — Invalid credentials". These accounts do not exist in the Staff Roster (confirmed by checking /staff as Owner — only manager, waiter, kitchen, accountant, waiter2, Cleaning, owner are listed).

**Root Cause:**
- The demo delivery agent user accounts have not been created in the database/auth system
- The Delivery & Online module (/delivery) shows Jamie Park, Carlos Mendez, and Priya Sharma as delivery agents — but these are display names, not actual system user accounts with login credentials
- There is no "delivery" role in the current staff user creation flow

**Expected Fix:**
1. Create three delivery agent user accounts in the database with the following credentials:
   - Username: `delivery1` | Password: `demo123` | Name: Carlos Mendez | Role: delivery_agent
   - Username: `delivery2` | Password: `demo123` | Name: Jamie Park | Role: delivery_agent
   - Username: `delivery3` | Password: `demo123` | Name: Priya Sharma | Role: delivery_agent
2. Ensure the `delivery_agent` role exists in the roles/permissions system
3. Create a Delivery Agent dashboard view (route: `/delivery-agent` or redirect to a delivery-specific view on login) with:
   - My Assigned Orders (list of orders assigned to this agent with: Order ID, Customer Name, Address, Phone, Items, Total)
   - Order lifecycle buttons: Accept → Picked Up → Out for Delivery → Delivered
   - My Delivery History (completed deliveries)
   - Earnings Summary (today / this week / this month)
4. Ensure the role-based sidebar for `delivery_agent` shows ONLY: Dashboard, My Deliveries, Delivery History, Earnings
5. Add `delivery_agent` as a selectable role in the "Add Staff" form in /staff so owners can create delivery agent accounts from the UI
6. Add a "Delivery" Quick Demo Access button on the login page alongside Owner / Manager / Waiter / Kitchen / Accountant

---

## MINOR UI FIXES (Include in the same plan)

### UI-FIX-01 — Currency Symbol in Cash Tendered Quick Buttons
**Location:** POS → Collect Payment modal → Cash Tendered quick-amount buttons
**Issue:** The quick-amount buttons show ₹50, ₹100, ₹200, ₹500 (Indian Rupee symbol ₹) instead of AED
**Fix:** Replace ₹ with AED in the button labels. Also make these quick-amount values contextual — for an AED restaurant, sensible quick amounts would be: AED 20, AED 50, AED 100, AED 200

### UI-FIX-02 — Bill Amount in Words Shows "Paise" Instead of "Fils"
**Location:** POS → Bill Preview modal → amount in words line
**Issue:** Bill shows "Thirty Five & 66 Paise" — "Paise" is the sub-unit of Indian Rupee. The correct sub-unit for UAE Dirham (AED) is "Fils"
**Fix:** In the `numberToWords()` or `amountToWords()` utility function, replace "Paise" with "Fils" when the restaurant currency is set to AED. Ideally make this dynamic based on the restaurant's currency setting stored in the database.

### UI-FIX-03 — "Cleaning" Staff Account Has Incorrect Role (Waiter)
**Location:** /staff → Roster
**Issue:** Charlotte (username: Cleaning) is assigned the "waiter" role but their job title is Cleaning staff. They should have a "cleaning" or "housekeeping" role, or at minimum their role badge should not show "waiter"
**Fix:** Either create a `cleaning_staff` role, or rename the display label for this account. Ensure cleaning staff do not have access to POS or order-taking